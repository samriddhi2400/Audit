<%- include("../partials/header") %>

<div class="container" style="min-height: 80vh;">
<div class="card"  style="width: 21.5em;margin:3rem auto;margin-top: 8rem;">
	<div class="card-body">
	  <h1 class="card-title text-center" style="margin-bottom:3rem" > Edit <%=audit.title%></h1>
	  <p class="card-text"><a href="/audit">back to audit</a></p>
      <form action="/audit/<%= audit._id%>?_method=PUT" method="POST">
	    <input type="text" value="<%= audit.title%>" style="margin-bottom: 3rem; margin-left: 3rem;" name="audit[title]">
	   <textarea cols="25" rows="7"name="audit[description]"  style="margin-bottom: 3rem; margin-left: 3rem;"><%= audit.description %></textarea>
	<button class="btn btn-primary"  style="margin-top: 1rem; margin-left: 6.5rem;">submit</button>
</form>
</div>
</div>
</div>
<%- include("../partials/footer") %>

